# Choses à corriger dans le code

## Tags

- Couleur
    - [X] Rendre la couleur de base aléatoire
- Unicité
    - [X] Pas de controle d'unicité ( les tags ne sont pas uniques )

## `task.controller.ts`

- LocalStorage
    - [ ] Vérifier la cohérrence
        - [ ] Gestion des clefs
        - [ ] Valeurs pas défaut
    - [ ] Simplifier les boucles de récupérations
        - Utiliser `getAllTasks()` pour éviter la duplication
    - [ ] Corriger les valeurs par défaut dans les boucles
        - ex: `last-id`
- JSON
    - [ ] Sécuriser les conversions
        - try / catch
- UX
    - [ ] Eviter les reloads systematiques
        - Préférer les callback ou events
- Typage
    - [ ] Typage explicite pour le retour des fonctions
    - [ ] Utiliser `===` au lieu de `==` ( type & valeur / valeur )
- Gestion des états
    - [ ] Nettoyer la gestion de l'état `tasksOpen`

## `tag.model.ts`

- Interface
    - Corriger la syntaxe
        - [ ] Manque de virgules dans l'interface
- JSON
    - [ ] Simplifier les conversions
        - [ ] JSON to Tag
        - [ ] Tag to JSON
    - [ ] Utiliser map pour transformer les objets JSON en Tag
- LocalStorage
    - [ ] Sécuriser la récupération depuis le localStorage
        - [ ] Types
        - [ ] Valeurs pas défault
- Typage
    - [ ] Ajouter des vérifications de types => eviter les erreurs

## `tasj.model.ts`

- Convertion String
    - [ ] `getArrayString` ajoute un saut de ligne après chaque tâche
        - Aussi à la dernière tâche
    - [ ] `getArrayString` utilise `task[i] || stringToTask("")`
        - Peut masquer des erreurs de données
    - [ ] `stringToTask` ne gère pas les cas où la chain n'a pas 5 éléments
        - Risques d'index out of bounds
    - [ ] Utiliser map et join pour plus de clarté dans `getArrayString`
- Task
    - [ ] `completedAt` devrait être null si la chaine est vide ou `null`